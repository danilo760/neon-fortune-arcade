diff --git a/scripts/goldenTigerDesktopSpinQa.mjs b/scripts/goldenTigerDesktopSpinQa.mjs
index e646281..6b76603 100644
--- a/scripts/goldenTigerDesktopSpinQa.mjs
+++ b/scripts/goldenTigerDesktopSpinQa.mjs
@@ -128,7 +128,7 @@ const auditExpression = `(() => {
       const hit = document.elementFromPoint(x, y);
       const reel = hit?.closest?.('.gt-hw-reel-overlay');
       const cell = hit?.closest?.('.gt-hw-cell');
-      const winOverlay = hit?.closest?.('.gt-hw-win-overlay');
+      const winOverlay = hit?.closest?.('.gt-hw-win-overlay, .gt-commercial-win');
       columns.push({
         column,
         x,
diff --git a/src/components/arcade/GoldenTigerCommercial.css b/src/components/arcade/GoldenTigerCommercial.css
index c94a2e0..9835530 100644
--- a/src/components/arcade/GoldenTigerCommercial.css
+++ b/src/components/arcade/GoldenTigerCommercial.css
@@ -31,6 +31,37 @@
   pointer-events: none;
 }
 
+.gt-scene-particles {
+  position: absolute;
+  z-index: 16;
+  inset: 0;
+  overflow: hidden;
+  opacity: 0;
+  transition: opacity .28s ease;
+}
+
+.gt-scene-particles i {
+  position: absolute;
+  left: var(--gt-scene-p-left);
+  top: var(--gt-scene-p-top);
+  width: var(--gt-scene-p-size);
+  height: var(--gt-scene-p-size);
+  border-radius: 50%;
+  background: rgba(255,224,132,.88);
+  box-shadow: 0 0 7px rgba(255,190,55,.72);
+  opacity: 0;
+}
+
+.gt-commercial-scene[data-lighting="feature"] .gt-scene-particles,
+.gt-commercial-scene[data-lighting="win"] .gt-scene-particles,
+.gt-commercial-scene[data-lighting="full"] .gt-scene-particles { opacity: 1; }
+
+.gt-commercial-scene[data-lighting="feature"] .gt-scene-particles i,
+.gt-commercial-scene[data-lighting="win"] .gt-scene-particles i,
+.gt-commercial-scene[data-lighting="full"] .gt-scene-particles i {
+  animation: gt-commercial-scene-particle var(--gt-scene-p-duration) ease-in-out var(--gt-scene-p-delay) infinite;
+}
+
 .gt-scene-layer {
   position: absolute;
   inset: 0;
@@ -110,10 +141,14 @@
   animation-delay: var(--gt-cell-stagger, 0ms) !important;
 }
 
-/* Lock feedback moves from overexposing art into the physical border. */
+/* Lock feedback stays peripheral so sticky symbol art remains legible. */
+.gt-hw-page .gt-premium-machine[data-feature-mode="active"] .gt-hw-cell.is-locked .gt-hw-symbol-raster {
+  filter: brightness(.92) saturate(.96) contrast(1.04) drop-shadow(0 4px 5px rgba(41,8,2,.28)) !important;
+}
+
 .gt-hw-page .gt-premium-machine[data-feature-mode="active"] .gt-hw-cell.is-fresh .gt-hw-symbol-raster {
   animation: gt-commercial-lock-symbol .38s cubic-bezier(.16,.84,.2,1.12) both !important;
-  filter: brightness(1.03) saturate(1.02) !important;
+  opacity: .88;
 }
 
 .gt-hw-page .gt-premium-machine .gt-premium-lock-ring {
@@ -138,9 +173,8 @@
   animation: gt-commercial-lock-edge .42s ease-out both;
 }
 
-.gt-hw-page .gt-premium-machine[data-feature-mode="active"] .gt-hw-cell.is-fresh .gt-hw-symbol-raster {
-  opacity: .78;
-  filter: brightness(.92) saturate(.96) !important;
+.gt-hw-page .gt-premium-machine[data-feature-mode="active"] .gt-hw-cell.is-fresh .gt-premium-lock-ring {
+  animation: gt-commercial-energy-frame .46s cubic-bezier(.16,.84,.2,1.12) both !important;
 }
 
 .gt-hw-page .gt-premium-machine .gt-premium-lock-ring > i {
@@ -259,7 +293,7 @@
 }
 
 .gt-commercial-win[data-win-beat="reveal"] .gt-commercial-win__dimmer,
-.gt-commercial-win[data-win-beat="celebrate"] .gt-commercial-win__dimmer { opacity: .82; }
+.gt-commercial-win[data-win-beat="celebrate"] .gt-commercial-win__dimmer { opacity: .68; }
 .gt-commercial-win[data-win-beat="reveal"] .gt-commercial-win__content {
   animation: gt-commercial-win-reveal .42s cubic-bezier(.18,.84,.22,1.12) both;
 }
@@ -298,6 +332,23 @@
   .gt-hw-page .gt-premium-machine .gt-hw-spin { width: 64px; }
 }
 
+@keyframes gt-commercial-scene-particle {
+  0% { opacity: 0; transform: translate3d(0,9px,0) scale(.7); }
+  24% { opacity: .72; }
+  76% { opacity: .42; }
+  100% { opacity: 0; transform: translate3d(5px,-28px,0) scale(1.12); }
+}
+
+@keyframes gt-commercial-energy-frame {
+  0% { opacity: .45; transform: scale(.93); }
+  52% {
+    opacity: 1;
+    transform: scale(1.025);
+    box-shadow: inset 0 0 0 1px rgba(142,62,8,.8), 0 0 0 2px rgba(255,218,105,.78), 0 0 14px rgba(255,170,35,.36);
+  }
+  100% { opacity: 1; transform: none; }
+}
+
 @keyframes gt-commercial-foreground {
   from { transform: translate3d(-1px,0,0) rotate(-.15deg); }
   to { transform: translate3d(1px,1px,0) rotate(.15deg); }
@@ -339,6 +390,7 @@
 
 @media (prefers-reduced-motion: reduce) {
   .layer-foreground,
+  .gt-scene-particles i,
   .gt-commercial-win *,
   .gt-premium-lock-ring::before,
   .gt-premium-lock-ring::after { animation: none !important; }
diff --git a/src/components/arcade/GoldenTigerPremium.tsx b/src/components/arcade/GoldenTigerPremium.tsx
index f06b69e..d5dcd2a 100644
--- a/src/components/arcade/GoldenTigerPremium.tsx
+++ b/src/components/arcade/GoldenTigerPremium.tsx
@@ -493,10 +493,13 @@ export function GoldenTigerPremium() {
 
       setWinBeat("impact");
       await wait(timeline.impactMs);
+      if (!clockRef.current) return;
       setWinBeat("reveal");
       await wait(timeline.revealMs);
+      if (!clockRef.current) return;
       setWinBeat("celebrate");
       await wait(timeline.celebrateMs);
+      if (!clockRef.current) return;
       setWinBeat(null);
     } else if (payout > visualStake) {
       setPhase("win");
@@ -731,7 +734,6 @@ export function GoldenTigerPremium() {
           <small>{featureActive ? `${lockedCount}/9 FIXOS · R${featureAttempt}` : "5 LINHAS FIXAS"}</small>
         </div>
 
-        <div className="gt-premium-feature-aura" aria-hidden />
         <div
           className="gt-hw-grid gt-premium-grid"
           data-landing-column={landingColumn}
diff --git a/src/components/arcade/golden-tiger/GoldenTigerScene25D.tsx b/src/components/arcade/golden-tiger/GoldenTigerScene25D.tsx
index 3ee055b..df4e066 100644
--- a/src/components/arcade/golden-tiger/GoldenTigerScene25D.tsx
+++ b/src/components/arcade/golden-tiger/GoldenTigerScene25D.tsx
@@ -1,7 +1,16 @@
+import type { CSSProperties } from "react";
 import { architecture, backplate, foreground, mascotPlatform, reelsFrame } from "./GoldenTigerSceneAssets";
 
 export type GoldenTigerLightingMode = "idle" | "spin" | "feature" | "win" | "full";
 
+const SCENE_PARTICLES = Array.from({ length: 12 }, (_, index) => ({
+  left: 8 + ((index * 31) % 84),
+  top: 18 + ((index * 23) % 66),
+  delay: -((index * 0.37) % 3.4),
+  duration: 2.8 + (index % 5) * 0.42,
+  size: 2 + (index % 3),
+}));
+
 export function GoldenTigerScene25D({ lighting }: { lighting: GoldenTigerLightingMode }) {
   return (
     <div className="gt-commercial-scene" data-lighting={lighting} aria-hidden>
@@ -12,6 +21,20 @@ export function GoldenTigerScene25D({ lighting }: { lighting: GoldenTigerLightin
       <img className="gt-scene-layer layer-foreground" src={foreground} alt="" draggable={false} />
       <span className="gt-scene-light gt-scene-light--ambient" />
       <span className="gt-scene-light gt-scene-light--spot" />
+      <span className="gt-scene-particles">
+        {SCENE_PARTICLES.map((particle, index) => (
+          <i
+            key={index}
+            style={{
+              "--gt-scene-p-left": `${particle.left}%`,
+              "--gt-scene-p-top": `${particle.top}%`,
+              "--gt-scene-p-delay": `${particle.delay}s`,
+              "--gt-scene-p-duration": `${particle.duration}s`,
+              "--gt-scene-p-size": `${particle.size}px`,
+            } as CSSProperties}
+          />
+        ))}
+      </span>
       <span className="gt-scene-vignette" />
     </div>
   );
