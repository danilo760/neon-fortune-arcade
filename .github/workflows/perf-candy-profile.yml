name: Perf Candy profile

on:
  push:
    branches: [perf/candy-render-cost]
    paths:
      - scripts/profile-candy-cdp.mjs

permissions:
  contents: read

jobs:
  profile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run typecheck
      - run: bun run test
      - run: bun run build
      - name: Profile Candy Auto at 390x844 / CPU 4x
        env:
          AFTER_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          cp scripts/profile-candy-cdp.mjs /tmp/profile-candy-cdp.mjs

          inject_counters() {
            python3 - <<'PY'
          from pathlib import Path
          path = Path('src/components/arcade/CandyCascadeHQ.tsx')
          text = path.read_text()
          main = 'export function CandyCascadeHQ() {\n'
          main_injected = '''export function CandyCascadeHQ() {\n  if (typeof window !== "undefined") {\n    const perfWindow = window as Window & { __candyRenderCount?: number; __candySymbolRenderCount?: number };\n    perfWindow.__candyRenderCount = (perfWindow.__candyRenderCount ?? 0) + 1;\n  }\n'''
          if main not in text:
              raise SystemExit('Candy main marker missing')
          text = text.replace(main, main_injected, 1)
          markers = [
              'function CandySymbol({ id }: { id: CandySymbolId }) {\n',
              'const CandySymbol = memo(function CandySymbol({ id }: { id: CandySymbolId }) {\n',
          ]
          injection = '''  if (typeof window !== "undefined") {\n    const perfWindow = window as Window & { __candySymbolRenderCount?: number };\n    perfWindow.__candySymbolRenderCount = (perfWindow.__candySymbolRenderCount ?? 0) + 1;\n  }\n'''
          for marker in markers:
              if marker in text:
                  text = text.replace(marker, marker + injection, 1)
                  break
          else:
              raise SystemExit('Candy symbol marker missing')
          path.write_text(text)
          PY
          }

          start_runtime() {
            bun run dev -- --host 127.0.0.1 --port 4173 >/tmp/candy-vite.log 2>&1 & VITE_PID=$!
            CHROME_BIN=$(command -v google-chrome || command -v chromium || command -v chromium-browser)
            "$CHROME_BIN" --headless=new --no-sandbox --disable-dev-shm-usage --autoplay-policy=no-user-gesture-required --remote-debugging-port=9222 --user-data-dir=/tmp/chrome-candy about:blank >/tmp/candy-chrome.log 2>&1 & CHROME_PID=$!
            for i in $(seq 1 120); do
              if curl -sf http://127.0.0.1:9222/json/version >/dev/null && curl -sf http://127.0.0.1:4173/game/candy-cascade >/dev/null; then return; fi
              sleep .25
            done
            cat /tmp/candy-vite.log || true; cat /tmp/candy-chrome.log || true; exit 1
          }

          stop_runtime() {
            kill "$VITE_PID" "$CHROME_PID" 2>/dev/null || true
            wait "$VITE_PID" 2>/dev/null || true
            wait "$CHROME_PID" 2>/dev/null || true
            rm -rf /tmp/chrome-candy
          }

          profile_ref() {
            REF="$1"; LABEL="$2"
            git checkout --force "$REF"
            bun install --frozen-lockfile >/dev/null
            inject_counters
            start_runtime
            node /tmp/profile-candy-cdp.mjs >"/tmp/$LABEL.json"
            stop_runtime
            echo "$LABEL=$(cat /tmp/$LABEL.json)"
          }

          profile_ref ac9b1f8b53aa3e9f99ac56104673e7997a7c6f4d before
          profile_ref "$AFTER_SHA" after

          node - <<'NODE'
          const fs = require('fs');
          const before = JSON.parse(fs.readFileSync('/tmp/before.json', 'utf8'));
          const after = JSON.parse(fs.readFileSync('/tmp/after.json', 'utf8'));
          const pct = (a,b) => a ? ((b-a)/a)*100 : 0;
          console.log('=== CANDY PERF COMPARISON ===');
          console.log(JSON.stringify({ before, after, delta: {
            avgFpsPct: pct(before.avgFps, after.avgFps),
            renderCountPct: pct(before.renderCount, after.renderCount),
            symbolRenderCountPct: pct(before.symbolRenderCount, after.symbolRenderCount),
            taskDurationPct: pct(before.taskDuration, after.taskDuration),
            scriptDurationPct: pct(before.scriptDuration, after.scriptDuration),
            longTasks: after.longTasks - before.longTasks,
            heapDeltaBytes: after.jsHeapDelta - before.jsHeapDelta,
          }}, null, 2));
          if (after.symbolRenderCount >= before.symbolRenderCount * 0.7) throw new Error('Candy symbol render reduction below 30% gate');
          if (after.scriptDuration > before.scriptDuration * 1.12) throw new Error('Candy script duration regressed >12%');
          NODE
